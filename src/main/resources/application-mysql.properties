# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://localhost/idus_martii
spring.datasource.username=idus_martii
spring.datasource.password=idus_martii
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
